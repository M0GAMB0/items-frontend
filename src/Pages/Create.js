import { Container } from "react-bootstrap";
import AddForm from "../Components/AddForm";

const Create = () => {
  return (
    <Container>
    //this is component we use to add items in list
      <AddForm />
    </Container>
  );
};
export default Create;
